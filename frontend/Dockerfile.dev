FROM node:22-alpine

WORKDIR /app

# Install dependencies first for caching
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose Vite internal port
EXPOSE 5173

# Run development server
CMD ["npm", "run", "dev"]
